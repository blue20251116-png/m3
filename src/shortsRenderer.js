import { mkdir, writeFile } from 'node:fs/promises';
import { spawn } from 'node:child_process';
import path from 'node:path';
import crypto from 'node:crypto';
const ROOT=process.env.WORK_DIR||'/tmp/m3-shorts';const OUTPUT_DIR=process.env.OUTPUT_DIR||path.join(process.cwd(),'public','renders');const FFMPEG_THREADS=String(Math.max(1,Math.min(4,Number(process.env.FFMPEG_THREADS||2))));
const DEFAULT_STYLE={title:{fontSize:62,color:'#FFFFFF',backgroundColor:'#000000',backgroundHeight:288,y:95,align:'center'},caption:{fontSize:54,color:'#FFFFFF',strokeColor:'#000000',strokeWidth:3,bottom:270,align:'center'}};
function run(cmd,args){return new Promise((resolve,reject)=>{const child=spawn(cmd,args,{stdio:['ignore','pipe','pipe']});let stderr='';child.stderr.on('data',d=>stderr+=d.toString());child.on('error',reject);child.on('close',(code,signal)=>code===0?resolve():reject(new Error(`${cmd} exited ${signal?`signal ${signal}`:`code ${code}`}: ${stderr.slice(-2200)}`)));});}
async function download(url,destination){const res=await fetch(url);if(!res.ok)throw new Error(`Download failed ${res.status}`);await writeFile(destination,Buffer.from(await res.arrayBuffer()));}
function esc(text=''){return String(text).replaceAll('\\','\\\\').replaceAll(':','\\:').replaceAll("'","\\'").replaceAll('%','\\%');}function clamp(v,min,max,f){const n=Number(v);return Number.isFinite(n)?Math.max(min,Math.min(max,n)):f;}function color(v,f){v=String(v||'').trim();return /^#[0-9a-fA-F]{6}$/.test(v)?`0x${v.slice(1)}`:`0x${f.slice(1)}`;}function alignX(a){return a==='left'?'55':a==='right'?'w-text_w-55':'(w-text_w)/2';}
function normalizeStyle(style={}){const title=style.title||{},caption=style.caption||{};return{title:{fontSize:clamp(title.fontSize,28,110,62),color:color(title.color,'#FFFFFF'),backgroundColor:color(title.backgroundColor,'#000000'),backgroundHeight:clamp(title.backgroundHeight,160,460,288),y:clamp(title.y,20,360,95),align:['left','center','right'].includes(title.align)?title.align:'center'},caption:{fontSize:clamp(caption.fontSize,24,100,54),color:color(caption.color,'#FFFFFF'),strokeColor:color(caption.strokeColor,'#000000'),strokeWidth:clamp(caption.strokeWidth,0,10,3),bottom:clamp(caption.bottom,80,700,270),align:['left','center','right'].includes(caption.align)?caption.align:'center'}};}
function clipDuration(c){const n=Number(c?.duration);return Number.isFinite(n)&&n>0?n:5;}
export async function renderThumbnail({clip,title,style={},timestamp=1}){if(!clip?.downloadUrl)throw new Error('clip is required');const s=normalizeStyle(style);await mkdir(ROOT,{recursive:true});await mkdir(OUTPUT_DIR,{recursive:true});const id=crypto.randomUUID();const jobDir=path.join(ROOT,`thumb-${id}`);await mkdir(jobDir,{recursive:true});const input=path.join(jobDir,'input.mp4');await download(clip.downloadUrl,input);const output=path.join(OUTPUT_DIR,`${id}.jpg`);const font=process.env.JP_FONT_FILE||'/usr/share/fonts/opentype/noto/NotoSansCJK-Bold.ttc';const videoHeight=Math.max(1200,1920-s.title.backgroundHeight);const filters=`scale=1080:${videoHeight}:force_original_aspect_ratio=increase,crop=1080:${videoHeight},pad=1080:1920:0:${s.title.backgroundHeight}:color=${s.title.backgroundColor},drawtext=fontfile='${font}':text='${esc(title)}':fontcolor=${s.title.color}:fontsize=${s.title.fontSize}:x=${alignX(s.title.align)}:y=${s.title.y}`;await run('ffmpeg',['-y','-threads',FFMPEG_THREADS,'-ss',String(Math.max(0,Number(timestamp)||0)),'-i',input,'-frames:v','1','-vf',filters,'-q:v','2',output]);return{id,filename:`${id}.jpg`,url:`/renders/${id}.jpg`,width:1080,height:1920};}
export async function renderShort({clips,title,captions=[],style={}}){if(!Array.isArray(clips)||clips.length<1)throw new Error('At least one clip is required');const s=normalizeStyle(style);await mkdir(ROOT,{recursive:true});await mkdir(OUTPUT_DIR,{recursive:true});const id=crypto.randomUUID(),jobDir=path.join(ROOT,id);await mkdir(jobDir,{recursive:true});const selected=clips.slice(0,6),durations=selected.map(clipDuration),totalDuration=durations.reduce((a,b)=>a+b,0),normalized=[],videoHeight=Math.max(1200,1920-s.title.backgroundHeight);console.log(`[RENDER ${id}] clips=${selected.length} duration=${totalDuration.toFixed(2)}s threads=${FFMPEG_THREADS}`);for(let i=0;i<selected.length;i++){const input=path.join(jobDir,`input-${i}.mp4`),out=path.join(jobDir,`clip-${i}.mp4`);await download(selected[i].downloadUrl,input);await run('ffmpeg',['-y','-threads',FFMPEG_THREADS,'-filter_threads',FFMPEG_THREADS,'-i',input,'-t',String(durations[i]),'-an','-vf',`scale=1080:${videoHeight}:force_original_aspect_ratio=increase,crop=1080:${videoHeight},fps=30,format=yuv420p`,'-c:v','libx264','-threads',FFMPEG_THREADS,'-preset','ultrafast','-crf','24','-pix_fmt','yuv420p','-movflags','+faststart',out]);normalized.push(out);}const concatFile=path.join(jobDir,'concat.txt');await writeFile(concatFile,normalized.map(p=>`file '${p.replaceAll("'","'\\''")}'`).join('\n'));const joined=path.join(jobDir,'joined.mp4');await run('ffmpeg',['-y','-threads',FFMPEG_THREADS,'-f','concat','-safe','0','-i',concatFile,'-c','copy',joined]);const output=path.join(OUTPUT_DIR,`${id}.mp4`),font=process.env.JP_FONT_FILE||'/usr/share/fonts/opentype/noto/NotoSansCJK-Bold.ttc';const captionFilters=captions.slice(0,5).map((line,idx)=>{const block=totalDuration/Math.max(captions.length,1),start=idx*block+Math.min(.6,block*.15),end=Math.min(totalDuration,(idx+1)*block-Math.min(.3,block*.08));return `drawtext=fontfile='${font}':text='${esc(line)}':fontcolor=${s.caption.color}:fontsize=${s.caption.fontSize}:borderw=${s.caption.strokeWidth}:bordercolor=${s.caption.strokeColor}:x=${alignX(s.caption.align)}:y=h-${s.caption.bottom}:enable='between(t,${start.toFixed(2)},${end.toFixed(2)})'`;});const filters=[`pad=1080:1920:0:${s.title.backgroundHeight}:color=${s.title.backgroundColor}`,`drawtext=fontfile='${font}':text='${esc(title)}':fontcolor=${s.title.color}:fontsize=${s.title.fontSize}:x=${alignX(s.title.align)}:y=${s.title.y}`,...captionFilters].join(',');await run('ffmpeg',['-y','-threads',FFMPEG_THREADS,'-filter_threads',FFMPEG_THREADS,'-i',joined,'-vf',filters,'-an','-c:v','libx264','-threads',FFMPEG_THREADS,'-preset','ultrafast','-crf','23','-pix_fmt','yuv420p','-movflags','+faststart',output]);return{id,filename:`${id}.mp4`,url:`/renders/${id}.mp4`,duration:Number(totalDuration.toFixed(2)),clipDurations:durations,threads:Number(FFMPEG_THREADS),style:s};}
